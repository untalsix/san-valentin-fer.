import streamlit as st

st.set_page_config(page_title="Para Fer López ❤️", page_icon="🌹")

st.markdown("""
    <style>
    .main {
        background-color: #fff0f3;
    }
    .stButton>button {
        width: 100%;
        border-radius: 25px;
        height: 3.5em;
        background-color: #ff4b6b;
        color: white;
        font-size: 20px;
        font-weight: bold;
        border: none;
        box-shadow: 2px 2px 10px rgba(0,0,0,0.1);
    }
    h1 {
        color: #ff4b6b;
        text-align: center;
        font-family: 'Helvetica';
    }
    p {
        text-align: center;
        font-size: 18px;
    }
    </style>
    """, unsafe_allow_html=True)

st.title("💌 Una nota especial...")

# Intenta cargar la foto de ustedes
try:
    st.image("nosotros.jpg", use_container_width=True)
except:
    st.warning("⚠️ Recuerda subir la foto con el nombre 'nosotros.jpg' al mismo lugar que este código.")

st.write("### Holaaaa, mi vida preciosa")
st.write("Hay algo que he querido preguntarte hace tiempo...")

st.markdown("## **¿Quieres ser mi San Valentín? Guapa ❤️**")

col1, col2 = st.columns(2)

with col1:
    if st.button("¡SÍ! 😍"):
        st.balloons()
        st.success("¡Me acabas de hacer la persona más feliz del mundo! 🌹✨")
        st.snow()

with col2:
    if st.button("No... 🥺"):
        st.error("¡Esa opción está deshabilitada por exceso de amor! Intenta de nuevo. 😉")
